import{describe,expect,it}from'vitest';import{applyParry,applyVerdict,damageForScore,newBattle,resolveParry,scoreForHitCount}from'./v2';
describe('V2 battle rules',()=>{it('resolves exact parry timing windows',()=>{expect(resolveParry(200)).toBe('Miss');expect(resolveParry(400)).toBe('Nice');expect(resolveParry(700)).toBe('Perfect');expect(resolveParry(850)).toBe('Miss')});it('applies parry damage, counter and meter',()=>{expect(applyParry(newBattle(),'Miss').playerHp).toBe(88);const perfect=applyParry(newBattle(),'Perfect');expect(perfect.bossHp).toBe(788);expect(perfect.meter).toBe(30)});it('maps verdict score and damage',()=>{expect(scoreForHitCount(true,4)).toBe(100);expect(scoreForHitCount(false,4)).toBe(0);expect(damageForScore(100)).toBe(640)});it('carves loot and applies verdict damage',()=>{const state={...newBattle(),meter:100};const result=applyVerdict(state,100);expect(result.bossHp).toBe(160);expect(result.remainingParts).toBe(1);expect(result.loot).toEqual([{quality:'Top',count:2}])});it('successful parry still damages after multiple verdicts',()=>{let state={...newBattle(),meter:100};state=applyVerdict(state,0);state={...state,meter:100};state=applyVerdict(state,0);const hp=state.bossHp;state=applyParry(state,'Perfect');expect(state.bossHp).toBe(hp-12)})});
