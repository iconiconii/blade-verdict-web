import{describe,expect,it}from'vitest';import{applyParry,applyVerdict,damageForScore,newBattle,resolveParry,resolveRingParry,ringLifecycleMs,ringRadiusAt,scoreForHitCount}from'./v2';
describe('V2 battle rules',()=>{it('resolves legacy timing windows for compatibility',()=>{expect(resolveParry(200)).toBe('Miss');expect(resolveParry(400)).toBe('Nice');expect(resolveParry(700)).toBe('Perfect');expect(resolveParry(850)).toBe('Miss')});it('accepts every click in the new ring lifetime and only misses after it disappears',()=>{expect(ringLifecycleMs).toBe(1800);expect(ringRadiusAt(0)).toBe(30);expect(Math.round(ringRadiusAt(600))).toBe(90);expect(Math.round(ringRadiusAt(1400))).toBe(75);expect(ringRadiusAt(1800)).toBe(0);expect(resolveRingParry(200)).toBe('Nice');expect(resolveRingParry(700)).toBe('Perfect');expect(resolveRingParry(1750)).toBe('Nice');expect(resolveRingParry(1800)).toBe('Miss')});it('applies parry damage and type-specific energy without draining on a miss',()=>{expect(applyParry({...newBattle(),meter:50},'Miss').playerHp).toBe(88);expect(applyParry({...newBattle(),meter:50},'Miss').meter).toBe(50);expect(applyParry(newBattle(),'Nice').meter).toBe(18);const perfect=applyParry(newBattle(),'Perfect');expect(perfect.bossHp).toBe(788);expect(perfect.meter).toBe(30)});it('maps verdict score and damage',()=>{expect(scoreForHitCount(true,4)).toBe(100);expect(scoreForHitCount(false,4)).toBe(0);expect(damageForScore(100)).toBe(640)});it('carves loot and applies verdict damage',()=>{const state={...newBattle(),meter:100};const result=applyVerdict(state,100);expect(result.bossHp).toBe(160);expect(result.remainingParts).toBe(1);expect(result.loot).toEqual([{quality:'Top',count:2}])});it('successful parry still damages after multiple verdicts',()=>{let state={...newBattle(),meter:100};state=applyVerdict(state,0);state={...state,meter:100};state=applyVerdict(state,0);const hp=state.bossHp;state=applyParry(state,'Perfect');expect(state.bossHp).toBe(hp-12)})});
